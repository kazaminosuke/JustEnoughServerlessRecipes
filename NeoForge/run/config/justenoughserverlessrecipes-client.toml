[transfer]
	#Minimum ticks between serverless recipe transfers (0 = unlimited, default: 1). Increase if the server kicks you for sending too many packets.
	# Default: 1
	# Range: 0 ~ 200
	cooldownTicks = 1
